# MetalX Common

## Build

The project use `just` as command runner, and installation refers to [casey/just](https://github.com/casey/just)

### Generate FlatBuffers

1. ensure you have `flatc` installed. e.g. `sudo dnf install -y flatbuffers-compiler` in Fedora, diffs between distros.
2. `just gen`
